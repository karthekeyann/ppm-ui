
/**
 * New typescript file
 */
export interface ParameterSet {
      number ? : number;
      name ? : string;
      applicationID ? : string;
      companyID ? : string;
      selected ? : boolean;
      effectiveDate ? : Date;

}
